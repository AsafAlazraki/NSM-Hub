/** @type {import('next').NextConfig} */
const nextConfig = {
  /*
    The Next.js team recommends against using the `devIndicators` configuration option.
    Instead, they suggest using the default development indicators provided by the framework.
    These indicators are designed to be unobtrusive and provide essential information
    during development without requiring manual configuration.

    If you still need to customize the development indicators, please refer to the
    official Next.js documentation for the latest recommended practices.
    https://nextjs.org/docs/api-reference/next.config.js/react-strict-mode
  */
};

export default nextConfig;
